{
  "name": "whos-that-pokemon-react",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "ms": "^2.0.0",
    "prop-types": "^15.5.10",
    "react": "^15.5.4",
    "react-bootstrap": "^0.31.0",
    "react-dom": "^15.5.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "git branch -D tmp; yarn build && git checkout -b tmp && git add -f build && git commit -m 'build' && git push -f heroku tmp:master && git checkout -",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
